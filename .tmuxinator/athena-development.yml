# ~/.tmuxinator/athena-development.yml

name: athena-development
root: ~/workspace/athena

windows:
  - editor:
     layout: main-vertical
     panes:
        - vim
        - #empty, will just run plain bash
  - console: #empty
  - logs: tail -f log/development.log
